import '../../domain/entities/register.dart';

class RegisterModel extends RegisterEntities{
  RegisterModel({ required super.name, required super.phone,
    required super.email, required super.password,});
}